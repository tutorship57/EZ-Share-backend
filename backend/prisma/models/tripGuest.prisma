model TripGuest{
    trip_id String
    guest_id String 
    total_spend Int
    @@id([trip_id, guest_id])
    trip_table Trip @relation(fields: [trip_id],references: [trip_id])
    guest_table Guest @relation(fields: [guest_id],references: [guest_id])
}